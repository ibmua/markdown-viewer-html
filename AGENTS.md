# Markdown viewer

Read [README.md](README.md), especially **Project structure and design**, before editing.
The app must remain a single offline-capable `index.html`; preserve inlined dependencies
and file saving/reloading behavior. CSS tokens in that file own presentation.
Test the real file URL in a browser, including light/dark and narrow screens.
